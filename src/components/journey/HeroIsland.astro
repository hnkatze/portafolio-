---
import type { PersonalInfo, Lang } from '@/lib/types';
import { t } from '@/lib/i18n';
import ParallaxLayer from './ParallaxLayer.astro';

interface Props {
  data: PersonalInfo;
  lang: Lang;
}

const { data, lang } = Astro.props;
---

<section id="hero" class="relative min-h-screen flex items-center justify-center overflow-hidden">
  <!-- Parallax background decorations -->
  <ParallaxLayer speed={-0.3} class="absolute inset-0 pointer-events-none">
    <div class="absolute top-20 left-10 w-72 h-72 bg-blue-500/5 rounded-full blur-3xl"></div>
    <div class="absolute bottom-20 right-10 w-96 h-96 bg-purple-500/5 rounded-full blur-3xl"></div>
    <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[600px] h-[600px] bg-blue-400/3 rounded-full blur-[100px]"></div>
  </ParallaxLayer>

  <ParallaxLayer speed={0.2} class="absolute inset-0 pointer-events-none">
    <div class="absolute top-[15%] right-[20%] w-2 h-2 bg-blue-400/40 rounded-full"></div>
    <div class="absolute top-[35%] left-[15%] w-1.5 h-1.5 bg-purple-400/30 rounded-full"></div>
    <div class="absolute bottom-[25%] right-[30%] w-1 h-1 bg-white/20 rounded-full"></div>
    <div class="absolute top-[60%] left-[25%] w-2.5 h-2.5 bg-blue-300/20 rounded-full"></div>
  </ParallaxLayer>

  <!-- Content -->
  <div class="relative z-10 max-w-5xl mx-auto px-6 text-center">
    <!-- Profile photo -->
    <div class="mb-8" data-hero-item="photo">
      <div class="relative inline-block">
        <div class="absolute inset-0 rounded-full bg-gradient-to-br from-blue-400 to-purple-500 blur-lg opacity-30 animate-glow-pulse"></div>
        <picture>
          <source srcset="/tshirtw.webp" type="image/webp" />
          <img
            src="/tshirtw.jpg"
            alt={data.name}
            width="800"
            height="800"
            class="relative w-32 h-32 md:w-40 md:h-40 rounded-full object-cover border-2 border-white/10"
            loading="eager"
            fetchpriority="high"
            decoding="async"
          />
        </picture>
      </div>
    </div>

    <!-- Name -->
    <h1
      class="text-4xl md:text-6xl lg:text-7xl font-bold text-white mb-4 cursor-default"
      data-hero-item="text"
      data-hero-name
    >
      {data.name}
    </h1>

    <!-- Title -->
    <p class="text-xl md:text-2xl text-blue-400 font-medium mb-6" data-hero-item="text" data-i18n-en={t(data.title, 'en')} data-i18n-es={t(data.title, 'es')}>
      {t(data.title, lang)}
    </p>

    <!-- Summary -->
    <p
      class="text-gray-400 text-base md:text-lg max-w-2xl mx-auto mb-10 leading-relaxed"
      data-hero-item="text"
      data-hero-summary
      data-i18n-en={t(data.summary, 'en')}
      data-i18n-es={t(data.summary, 'es')}
    >
      {t(data.summary, lang)}
    </p>

    <!-- Social links -->
    <div class="flex items-center justify-center gap-4" data-hero-item="text">
      <a
        href={`https://github.com/${data.socials.github}`}
        target="_blank"
        rel="noopener noreferrer"
        data-magnetic
        class="p-3 rounded-xl bg-white/5 border border-white/10 text-gray-400 hover:text-white hover:border-blue-400/50 hover:bg-white/10 transition-all duration-300"
        aria-label="GitHub"
      >
        <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24">
          <path d="M12 0c-6.626 0-12 5.373-12 12 0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23.957-.266 1.983-.399 3.003-.404 1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576 4.765-1.589 8.199-6.086 8.199-11.386 0-6.627-5.373-12-12-12z" />
        </svg>
      </a>
      <a
        href={data.socials.linkedin}
        target="_blank"
        rel="noopener noreferrer"
        data-magnetic
        class="p-3 rounded-xl bg-white/5 border border-white/10 text-gray-400 hover:text-white hover:border-blue-400/50 hover:bg-white/10 transition-all duration-300"
        aria-label="LinkedIn"
      >
        <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24">
          <path d="M20.447 20.452h-3.554v-5.569c0-1.328-.027-3.037-1.852-3.037-1.853 0-2.136 1.445-2.136 2.939v5.667H9.351V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85 3.601 0 4.267 2.37 4.267 5.455v6.286zM5.337 7.433c-1.144 0-2.063-.926-2.063-2.065 0-1.138.92-2.063 2.063-2.063 1.14 0 2.064.925 2.064 2.063 0 1.139-.925 2.065-2.064 2.065zm1.782 13.019H3.555V9h3.564v11.452zM22.225 0H1.771C.792 0 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 24 23.227 24 22.271V1.729C24 .774 23.2 0 22.222 0h.003z" />
        </svg>
      </a>
      <a
        href={`mailto:${data.email}`}
        data-magnetic
        class="p-3 rounded-xl bg-white/5 border border-white/10 text-gray-400 hover:text-white hover:border-blue-400/50 hover:bg-white/10 transition-all duration-300"
        aria-label="Email"
      >
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="1.5">
          <path stroke-linecap="round" stroke-linejoin="round" d="M21.75 6.75v10.5a2.25 2.25 0 01-2.25 2.25h-15a2.25 2.25 0 01-2.25-2.25V6.75m19.5 0A2.25 2.25 0 0019.5 4.5h-15a2.25 2.25 0 00-2.25 2.25m19.5 0v.243a2.25 2.25 0 01-1.07 1.916l-7.5 4.615a2.25 2.25 0 01-2.36 0L3.32 8.91a2.25 2.25 0 01-1.07-1.916V6.75" />
        </svg>
      </a>
    </div>
  </div>

  <!-- Scroll indicator -->
  <div
    data-hero-scroll-indicator
    class="absolute bottom-8 left-1/2 -translate-x-1/2 flex flex-col items-center gap-2 text-gray-500 pointer-events-none"
    aria-hidden="true"
  >
    <span class="text-[10px] uppercase tracking-[0.2em]">Scroll</span>
    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2">
      <path stroke-linecap="round" stroke-linejoin="round" d="M19 14l-7 7m0 0l-7-7m7 7V3" />
    </svg>
  </div>
</section>

<script>
  import { animate, utils, magnetic, prefersReducedMotion } from '@/lib/anime';
  import { split, stagger } from 'animejs';

  // Apply magnetic effect to all marked CTAs in the Hero
  document.querySelectorAll<HTMLElement>('#hero [data-magnetic]').forEach((el) => {
    magnetic(el, { strength: 0.35, radius: 90 });
  });

  const photo = document.querySelector<HTMLElement>('[data-hero-item="photo"]');
  const textItems = document.querySelectorAll<HTMLElement>('[data-hero-item="text"]');
  const nameEl = document.querySelector<HTMLElement>('[data-hero-name]');
  const summaryEl = document.querySelector<HTMLElement>('[data-hero-summary]');
  const scrollIndicator = document.querySelector<HTMLElement>('[data-hero-scroll-indicator]');
  const durationMs = prefersReducedMotion ? 0 : 500;

  // --- Photo entrance ---
  if (photo) {
    utils.set(photo, { scale: 0.8, opacity: 0 });
    animate(photo, {
      scale: [0.8, 1],
      opacity: [0, 1],
      duration: durationMs,
      ease: 'outQuad',
    });
  }

  // --- Generic text items entrance (title + social row) ---
  textItems.forEach((el, i) => {
    if (el === nameEl || el === summaryEl) return;
    utils.set(el, { opacity: 0, translateY: 20 });
    animate(el, {
      opacity: [0, 1],
      translateY: [20, 0],
      duration: durationMs,
      delay: (i + 1) * 150,
      ease: 'outQuad',
    });
  });

  // --- Glitch effect on Hero name ---
  function runGlitch(target: HTMLElement, jitterPx: number, duration: number) {
    if (prefersReducedMotion) {
      utils.set(target, { opacity: 1 });
      return;
    }
    const splitter = split(target, { chars: { wrap: 'clip' } });
    const chars = splitter.chars as HTMLElement[];
    if (chars.length === 0) return;

    utils.set(chars, { opacity: 0 });

    // Phase 1: chaotic jitter while fading in
    animate(chars, {
      opacity: [0, 1],
      translateX: () => (Math.random() - 0.5) * jitterPx,
      translateY: () => (Math.random() - 0.5) * jitterPx,
      duration: duration * 0.6,
      delay: stagger(20),
      ease: 'outQuad',
    });

    // Phase 2: settle to 0
    animate(chars, {
      translateX: 0,
      translateY: 0,
      duration: duration * 0.4,
      delay: stagger(15, { start: duration * 0.6 }),
      ease: 'outElastic(1, .6)',
    });
  }

  if (nameEl) {
    utils.set(nameEl, { opacity: 0 });
    setTimeout(() => {
      utils.set(nameEl, { opacity: 1 });
      runGlitch(nameEl, 12, 700);
    }, 200);

    // Re-glitch on hover (debounced — ignore while running)
    let glitching = false;
    nameEl.addEventListener('mouseenter', () => {
      if (glitching || prefersReducedMotion) return;
      glitching = true;
      runGlitch(nameEl, 8, 500);
      setTimeout(() => { glitching = false; }, 500);
    });
  }

  // --- Typing effect on Hero summary ---
  function runTyping(target: HTMLElement, lang: 'en' | 'es' = 'en') {
    if (!target) return;
    const text = target.getAttribute(`data-i18n-${lang}`) || target.textContent || '';
    target.textContent = text;

    if (prefersReducedMotion) {
      utils.set(target, { opacity: 1 });
      return;
    }

    const splitter = split(target, { chars: { wrap: 'clip' } });
    const chars = splitter.chars as HTMLElement[];
    utils.set(chars, { opacity: 0 });
    utils.set(target, { opacity: 1 });

    animate(chars, {
      opacity: [0, 1],
      duration: 1,
      delay: stagger(15),
      ease: 'linear',
    });
  }

  if (summaryEl) {
    utils.set(summaryEl, { opacity: 0 });
    const initialLang = (localStorage.getItem('lang') as 'en' | 'es') || 'en';
    setTimeout(() => runTyping(summaryEl, initialLang), 600);

    // Re-trigger on language change
    window.addEventListener('lang-change', (e) => {
      const lang = (e as CustomEvent).detail as 'en' | 'es';
      runTyping(summaryEl, lang);
    });
  }

  // --- Scroll indicator: bouncing arrow that fades on first scroll ---
  if (scrollIndicator) {
    if (!prefersReducedMotion) {
      animate(scrollIndicator, {
        translateY: [0, 8, 0],
        duration: 1500,
        loop: true,
        ease: 'inOutSine',
      });
    }

    let dismissed = false;
    const dismiss = () => {
      if (dismissed) return;
      dismissed = true;
      animate(scrollIndicator, {
        opacity: [1, 0],
        translateY: 20,
        duration: 400,
        ease: 'outQuad',
        onComplete: () => scrollIndicator.remove(),
      });
      window.removeEventListener('scroll', onScroll);
    };
    const onScroll = () => {
      if (window.scrollY > 50) dismiss();
    };
    window.addEventListener('scroll', onScroll, { passive: true });
  }
</script>
